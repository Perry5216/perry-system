# Environment and Secrets
.env
secrets/
.cloudflared-my-n8n.json
my-n8n-config.yml
n8nac-config.json

# Worker config files (may contain auth tokens / OAuth state)
perry/worker/.mcp.json
perry/worker/gemini-settings.json

# VPN config files (contain WireGuard / OpenVPN credentials)
perry/config/openvpn/

# Belt-and-braces for sensitive config dirs
perry/config/.vault/
perry/config/user.json

# Large model files and local DBs
.ollama-models/
qdrant_storage/
postgres_data/

# Docker bind-mount volumes (ollama, comfyui, trainer hf cache)
data/

# Claude Code harness metadata
.claude/

# Runtime dumps and ad-hoc prompt files
projects.json
project-*-data.json
step*-prompt.txt
perry/tmp_logs.txt
perry/config/user.json.bak-*

# Local logs and temporary files
perry_logs.txt
telemetry.txt
telemetry_dump.txt
temp_scene.txt
*.log

# Node and VSCode
node_modules/
.vscode/

# Ignore heavy output files inside the perry app
perry/node_modules/
perry/scratch/
perry/logs.txt
perry/workspace_style_dna.json

# Massive AI Models and Tensor weights in Workspace
*.safetensors
*.gguf
*.pth
*.pt
*.bin
*.db-wal
*.db-shm
perry/workspace/training/*/lora-adapter/
perry/workspace/training/*/gguf/
